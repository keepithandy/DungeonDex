DungeonDex v1.3.38 - Progression Clarity Pass Smoke Notes

Scope:
Static/runtime syntax checks plus one local headless DOM load were performed. Manual interactive browser testing was not performed.

Validation performed:
- JavaScript syntax check: PASS
  Command: node --check app.js
- Service worker syntax check: PASS
  Command: node --check sw.js
- Manifest JSON parse: PASS
  Command: Get-Content -Raw -Path manifest.json | ConvertFrom-Json | Out-Null
- Static asset reference check: PASS
  index.html references manifest.json, styles.css, sw.js, and app.js. sw.js caches the same root runtime files with current v1.3.38 cache-busting strings.
- Runtime file presence check: PASS
  app.js, index.html, styles.css, sw.js, and manifest.json are present in the repo root.
- Stale v1.3.37 runtime label search: PASS
  No v1.3.37, DungeonDex v1.3.37, or 1.3.37-mobile runtime labels remain in app.js, index.html, styles.css, sw.js, or manifest.json.
- Long visible build banner search: PASS
  No EMBERFALL BUILD or BUILD DUNGEONDEX banner text remains in runtime files.
- Visible label check: PASS
  Source and headless DOM show DungeonDex v1.3.38.
- Service worker cache label check: PASS
  Cache name is dungeon-dex-v1.3.38-progression-clarity-pass.
- Level wording audit: PASS
  Remaining level uses are player level, item level/ilvl, monster level, risk level, sort keys, or CSS item-level selectors. No dungeon-progression level wording was found requiring conversion.
- Boss/floor/extract/defeat wording audit: PASS
  Source now leads with Best Floor, Safe Return, Next Boss, Boss Floor, Extraction secured/failed, Defeated, Lost unsecured rewards, and banked progress language.
- Combat/action selector check: PASS
  Attack, Skill, Guard, Extract, combat button selectors, Enter Dungeon, Continue Run, Rest, Merchant, Inventory, and Archive source hooks remain present.
- Headless local DOM load: PASS
  Chrome headless rendered the app shell, short visible version label, Lowfire town screen, intro modal content, merchant, inventory, archive, idle run panel, and updated progression copy.

Manual/static smoke checklist:
- App shell loads: PASS via headless DOM load.
- Visible label is short, DungeonDex v1.3.38: PASS.
- Intro modal still works if present: STATIC PASS. Modal content and buttons render; interactive click-through was not performed.
- Enter Dungeon remains available: PASS by source and headless DOM.
- Continue Run remains available when appropriate: STATIC PASS by source wiring.
- Current floor/depth information is clearer: PASS by source and headless DOM.
- Boss floor rhythm is clearer: PASS by source.
- Extraction consequence is clearer: PASS by source.
- Death/defeat consequence is clearer: PASS by source.
- Return-to-Lowfire copy is clearer: PASS by source and headless DOM.
- Archive/run history is clearer: PASS by source and headless DOM.
- Combat actions remain present: STATIC PASS by source selectors.
- Merchant opens/renders: PASS by headless DOM render and source wiring.
- Inventory opens/renders: PASS by headless DOM render and source wiring.
- Archive opens/renders: PASS by headless DOM render and source wiring.
- No obvious mobile text clutter regression: STATIC PASS by scoped copy review. Real mobile viewport/device testing was not performed.
- Service worker cache label is current: PASS.

Checks not performed:
- Manual interactive click-through in a visible browser was not performed.
- Full active combat, extraction, victory, defeat, merchant buy/sell, and inventory equip flows were not manually played.
- Real iOS Safari, installed PWA, and itch iframe touch scrolling were not manually tested.
- Full balance, economy, scaling, and save migration testing were not performed because this pass intentionally avoids those systems.

Remaining risks:
- Static and headless checks verify syntax, source wiring, and a local DOM render, not actual touch ergonomics on mobile hardware.
- Older saved logs/history entries may still contain older wording until new runs replace them naturally.
- Some internal save/runtime names still use depth/floor field names for compatibility; this pass changes display wording only.
