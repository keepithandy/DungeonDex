DungeonDex v1.3.37 - Mobile Dungeon UI Polish Smoke Notes

Scope:
Static/runtime syntax checks plus one headless local DOM load were performed. Manual interactive browser testing was not performed.

Validation performed:
- JavaScript syntax check: PASS
  Command: node --check app.js
- Service worker syntax check: PASS
  Command: node --check sw.js
- Manifest JSON parse: PASS
  Command: Get-Content manifest.json -Raw | ConvertFrom-Json | Out-Null
- Static asset reference check: PASS
  index.html references manifest.json, styles.css, sw.js, and app.js with current v1.3.37 cache-busting strings; those files exist in the repo root.
- Stale v1.3.36 runtime label and long banner search: PASS
  app.js, index.html, sw.js, and manifest.json have no v1.3.36 runtime labels and no EMBERFALL BUILD / BUILD DUNGEONDEX banner text after the update.
- Visible label check: PASS
  Source and headless DOM show DungeonDex v1.3.37.
- Service worker cache label check: PASS
  Cache name is dungeon-dex-v1.3.37-mobile-dungeon-ui-polish.
- CSS overflow/body/html review: PASS
  Existing itch/iOS scroll hardening remains intentional. The v1.3.37 block does not add new body/html overflow rules.
- Combat/action selector check: PASS
  Attack, Skill, Guard, Extract, combat button, combat log, HP card, and combat action selectors remain present.
- Core play UI preservation check: PASS
  Enter Dungeon, Continue Run, Rest, Merchant, Inventory, Archive, combat actions, and Extract remain present by source search.
- Headless local DOM load: PASS
  Chrome headless rendered the app shell, Lowfire town screen, intro modal content, merchant, inventory, archive, idle run panel, and visible label DungeonDex v1.3.37.

Manual/static smoke checklist:
- App shell loads: PASS via headless DOM load.
- Visible label is short, DungeonDex v1.3.37: PASS.
- Intro modal still works if present: STATIC PASS. Modal content and buttons render; interactive click-through was not performed.
- Enter Dungeon remains available: PASS by source and headless DOM.
- Continue Run remains available when appropriate: STATIC PASS by source and existing continueRun wiring.
- Combat actions remain present: STATIC PASS by source.
- Attack button is visible and tappable: STATIC PASS by source/CSS selectors.
- Skill button is visible and tappable: STATIC PASS by source/CSS selectors.
- Guard button is visible and tappable: STATIC PASS by source/CSS selectors.
- Extract button is visible and tappable: STATIC PASS by source/CSS selectors.
- Combat log remains readable: STATIC PASS by CSS/source review; idle feed rendered in headless DOM.
- HP/status information remains readable: STATIC PASS by CSS/source review.
- Merchant opens/renders: PASS by headless DOM render and source wiring.
- Inventory opens/renders: PASS by headless DOM render and source wiring.
- Archive opens/renders: PASS by headless DOM render and source wiring.
- No obvious mobile overflow regression: STATIC PASS by scoped CSS review. A mobile-sized browser viewport was not completed.
- Service worker cache label is current: PASS.

Checks not performed:
- Manual interactive click-through in a visible browser was not performed.
- Full active combat, extraction, victory, defeat, merchant buy/sell, and inventory equip flows were not manually played.
- Real iOS Safari, installed PWA, and itch iframe touch scrolling were not manually tested.
- A mobile-sized headless Chrome pass was attempted but did not complete because the sandbox hit a transient spawn setup error.
- Full balance, economy, scaling, and save migration testing were not performed because this pass intentionally avoids those systems.

Remaining risks:
- Static checks verify syntax, source wiring, and one local DOM render, not full touch ergonomics on actual mobile hardware.
- Older saved run logs may still contain older wording until new entries replace them naturally.
- The combat log now renders 3 recent entries instead of 2. This is presentation-only but should be watched on very short screens.
