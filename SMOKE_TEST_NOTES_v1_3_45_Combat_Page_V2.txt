DungeonDex v1.3.45 — Combat Page V2 — Smoke Test Notes

Test environment:
- Open index.html directly in a browser (Chrome, Safari, Firefox).
- Also test on iPhone Safari or iOS browser if available.
- No server required. Single-file local open is fine.

─── Core load checks ───────────────────────────────────────

[ ] App loads without console errors.
[ ] Title bar / version label reads: DungeonDex v1.3.45
[ ] Lowfire town screen renders on load.
[ ] Hero panel and tabs visible.
[ ] Enter Dungeon button present and tappable.

─── Combat flow checks ─────────────────────────────────────

[ ] Click Enter Dungeon — run screen loads.
[ ] Combat page renders cleanly: enemy name, HP bars, stat grid, action buttons all visible.
[ ] No horizontal overflow / no horizontal scrollbar.
[ ] Monster stage (silhouette area) is visible.

[ ] Click Attack — combat resolves, combat log updates.
[ ] Click Attack rapidly 5–10 times — buttons do not jump around under the thumb/cursor.
[ ] Combat log scrolls internally when it fills up; action buttons remain at bottom of panel.

[ ] Guard button works (resolves a guard tick, log updates).
[ ] Skill button works if skill is available (or shows correctly disabled/ghosted if not).
[ ] Extract button works if available (triggers extraction attempt).

[ ] Rewards still appear after a kill (loot/gold reward text shows in log or reward area).
[ ] After reward, next enemy loads and combat continues.

─── Layout stability checks ────────────────────────────────

[ ] Spam-clicking Attack at low level (floor 1–3) does not cause major vertical layout shifts.
[ ] Combat log area scrolls when entries fill the visible height, without pushing action buttons.
[ ] Enemy header (name, tier, family text) remains readable and not clipped to zero.
[ ] HP bars remain visible and update correctly.
[ ] Player stat grid (PWR / GRD / SPD / LCK) remains visible during combat.

─── Mobile checks (iPhone / narrow viewport) ───────────────

[ ] At ~375px wide (iPhone SE / standard iPhone):
    - Action buttons (Attack / Skill / Guard / Extract) remain fully visible and tappable.
    - Monster stage visible (not hidden/collapsed).
    - Combat log area present and scrollable.
    - No content clipped off-screen.
    - Text is legible (not microscopic).
[ ] No horizontal scroll on mobile viewport.

─── Town / merchant / inventory checks ─────────────────────

[ ] After extracting or dying, return to town screen works.
[ ] Merchant panel opens and functions.
[ ] Inventory / Gear tab opens and shows items.
[ ] All five nav tabs (Lowfire / Run / Gear / Dex / Archive) still function.

─── Tool / save checks ─────────────────────────────────────

[ ] python tools/check_dungeondex_package.py — PASS (no missing files, no warnings).
[ ] python tools/simulate_deep_scaling.py — runs without error (same pre-existing charter warnings OK).
[ ] python tools/audit_content_registries.py — runs without error.
[ ] node --check app.js — PASS.
[ ] node --check sw.js — PASS.
[ ] No save migration required — existing saves load without errors.
[ ] localStorage keys unchanged — opening with an existing save does not corrupt or reset progress.

─── Version label check ────────────────────────────────────

[ ] Header / topbar shows: DungeonDex v1.3.45  (short label only, no "Combat Page V2" suffix).
[ ] Settings / Archive panel shows DungeonDex v1.3.45.

─── Regression guard ───────────────────────────────────────

[ ] Combat math (damage, guard reduction) unchanged vs v1.3.44b.
[ ] Enemy HP and power at D1, D5, D10 appear the same as before.
[ ] Gold and shard rewards after kills appear the same as before.
[ ] Loot drop behavior appears the same as before.
[ ] Boss floor still triggers correctly at the expected floor.
[ ] Elite enemies still show modifier tags.
