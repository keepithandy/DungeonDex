DungeonDex v1.3.47d — Smoke Test Notes

Checks performed:
- app.js syntax check passed.
- index.html parses as plain HTML package content.
- Fallback build tag now shows DungeonDex v1.3.47d.
- app.js BUILD and VISIBLE_VERSION_LABEL now show DungeonDex v1.3.47d.
- Settings panel uses VISIBLE_VERSION_LABEL instead of stale hardcoded v1.3.47b text.
- The old “Lowfire return” pill text was changed to “Safe return.”
- index.html and sw.js now use the v1.3.47d-version-label-copy-cleanup cache string.

Manual checks recommended:
1. Load the game fresh and confirm the visible build label shows DungeonDex v1.3.47d.
2. Open Settings and confirm the settings version copy shows DungeonDex v1.3.47d.
3. Confirm the settings/status pill says Safe return.
4. Extract from Ashgate or deeper and confirm the town return panel still shows the staged district.
5. Start combat on phone portrait and confirm the v1.3.47b compact combat layout is unchanged.
